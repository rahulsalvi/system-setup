---
extends: default
rules:
  braces:
    max-spaces-inside: 1
  comments:
    min-spaces-from-content: 1
  comments-indentation: disable
  line-length:
    max: 120
  octal-values: enable
  truthy:
    allowed-values: ["true", "false", "yes", "no", "on", "off"]
