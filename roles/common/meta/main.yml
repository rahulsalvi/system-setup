---
dependencies:
  - role: pacman
    when: ansible_facts['distribution'] == "Archlinux"
  - role: systemd_resolved
  - role: ufw
  - role: ssh
  - role: tailscale
  - role: ansible
  - role: terminal
    when: ansible_facts['distribution'] == "Archlinux"
  - role: tls
