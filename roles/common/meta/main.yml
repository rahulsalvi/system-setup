---
dependencies:
  - role: pacman
  - role: systemd-resolved
  - role: ufw
  - role: ssh
    vars:
      enable_agent: "{{ ssh_enable_agent }}"
  - role: tailscale
  - role: ansible
  - role: terminal
  - role: tls
  - role: apparmor
  - role: lockdown
